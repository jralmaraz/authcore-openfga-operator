apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: genai-rag-agent-ingress
  namespace: default
  labels:
    app: genai-rag-agent
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/cors-allow-origin: "*"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, PUT, PATCH, DELETE, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-headers: "DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range,Authorization,x-user-id,x-user-role,x-user-email"
spec:
  ingressClassName: nginx
  rules:
  - host: genai-rag-agent.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: genai-rag-agent-service
            port:
              number: 80